<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 120">
  <!-- Leagues of Votann - Ancestor Core Symbol -->
  <!-- Top crown/chevron -->
  <path fill="currentColor" d="M50 5 L65 20 L60 20 L50 12 L40 20 L35 20 Z"/>

  <!-- Upper side chevrons (brow area) -->
  <path fill="currentColor" d="M25 25 L40 35 L40 42 L25 32 Z"/>
  <path fill="currentColor" d="M75 25 L60 35 L60 42 L75 32 Z"/>
  <path fill="currentColor" d="M20 35 L35 45 L35 52 L20 42 Z"/>
  <path fill="currentColor" d="M80 35 L65 45 L65 52 L80 42 Z"/>

  <!-- Central vertical strip (forehead to nose) -->
  <path fill="currentColor" d="M45 20 L55 20 L55 55 L45 55 Z"/>

  <!-- Eye diamonds -->
  <path fill="currentColor" d="M30 50 L40 58 L30 66 L20 58 Z"/>
  <path fill="currentColor" d="M70 50 L80 58 L70 66 L60 58 Z"/>

  <!-- Side panels -->
  <path fill="currentColor" d="M15 55 L25 55 L25 85 L15 85 Z"/>
  <path fill="currentColor" d="M75 55 L85 55 L85 85 L75 85 Z"/>

  <!-- Central nose bridge -->
  <path fill="currentColor" d="M45 55 L55 55 L55 75 L50 80 L45 75 Z"/>

  <!-- Beard vertical lines -->
  <path fill="currentColor" d="M30 70 L36 70 L36 100 L33 105 L30 100 Z"/>
  <path fill="currentColor" d="M64 70 L70 70 L70 100 L67 105 L64 100 Z"/>

  <!-- Central beard section -->
  <path fill="currentColor" d="M42 82 L58 82 L58 105 L50 115 L42 105 Z"/>

  <!-- Inner beard cutout -->
  <path fill="none" stroke="currentColor" stroke-width="0">
    <animate attributeName="d" values="M47 88 L53 88 L53 100 L50 105 L47 100 Z"/>
  </path>
</svg>
